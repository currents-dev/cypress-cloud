// type definitions for Cypress object "cy"
/// <reference types="cypress" />

describe("Retry", function () {
  // a very simple example helpful during presentations
  it("Wait 5 sec", function () {
    cy.wait(10000);
  });
});
